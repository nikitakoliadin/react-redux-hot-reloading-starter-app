import React from 'react';

import HeaderContainer from '../containers/HeaderContainer';

const Header = () => (
    <header className="App-header">
        React Redux Hot Reloading Starter App
        <HeaderContainer/>
    </header>
);

export default Header;
